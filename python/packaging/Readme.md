
Packaging python packages allows it to be shipped as a separate package that can be installed from pypi directly instead of having to worry about other dependencies and management.



poetry add $(cat requirements.txt)
