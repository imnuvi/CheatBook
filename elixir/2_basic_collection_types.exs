
# Tuples

my_tuple = { 1, :two, "three" }
tuple2 = { :ok, 55, "go" }
{ name, mark, prize } = { "ham", 50, :first }

IO.puts(inspect my_tuple)
IO.puts(inspect tuple2)
IO.puts(name)
IO.puts(mark)
IO.puts(prize)


# Lists
